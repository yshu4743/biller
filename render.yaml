version: "1"

services:
  # ---------------- Single Web Service (API + Frontend) ----------------
  - type: web
    name: biller-np48
    runtime: node
    plan: free
    region: oregon
    healthCheckPath: /
    buildCommand: |
      cd backend && npm install
      cd ../frontend && npm install && npm run build
    startCommand: node backend/server.js
    autoDeployTrigger: commit
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_EXPIRE
        value: 7d
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        value: 9b57e9e444f3d005d0ca5bb240c509057ba23dcd037bb4b34a04fd5d3df3eeef